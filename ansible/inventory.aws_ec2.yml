plugin: amazon.aws.aws_ec2
regions:
  - us-west-1
filters:
  tag:Tier:
    - public
    - private
hostnames:
  - ip-address
  - private-ip-address
groups:
  public: ec2_tags.Tier == 'public'
  private: ec2_tags.Tier == 'private'
